input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend :Convert
        type Converter interface {
        }
error: |-
    Error while parsing extend in
        github.com/polpo-studio/goverter/execution.Converter

    package path must not be empty in the extend statement ":Convert".
    See https://github.com/polpo-studio/goverter#extend-with-custom-implementation
