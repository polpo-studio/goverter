input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend strconv:ParseSomething
        type Converter interface {
        }
error: |-
    Error while parsing extend in
        github.com/polpo-studio/goverter/execution.Converter

    could not extend: ParseSomething does not exist in scope
