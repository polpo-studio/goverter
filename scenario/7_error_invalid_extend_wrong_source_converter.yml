input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend HelloWorld
        type Converter interface {
        }
        func HelloWorld(int, int) int {
            return 0
        }
error: |-
    Error while parsing extend in
        github.com/polpo-studio/goverter/execution.Converter

    the first parameter must be of type github.com/polpo-studio/goverter/execution.Converter
